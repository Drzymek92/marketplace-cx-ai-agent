# marketplace-cx-ai-agent — runtime dependencies (pinned).
# Reproduce in a clean venv with:  pip install -r requirements.txt

# GraphQL (data layer — serve + consume)
strawberry-graphql==0.317.1
graphql-core==3.2.11

# Web server (serves the GraphQL endpoint) + HTTP client (consumes it)
fastapi==0.115.5
uvicorn==0.32.1
httpx==0.27.0

# Agent stack (agent core, tool-calling) + OpenAI-compatible LLM client
langgraph==1.0.10
langchain==1.2.10
langchain-openai==1.1.10
langchain-core==1.2.17
python-dotenv==0.21.0

# Config + validation
pydantic==2.10.1
PyYAML==6.0.1
