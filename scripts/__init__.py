"""marketplace-cx-agent source package.

NOTE: a `scripts` package may also exist elsewhere on the Python path (some installed
distributions ship one) and can shadow this local one. Runnable entry points and tests
insert the project root at sys.path[0] so this package wins. See
scripts/graphql_server/app.py and tests/conftest.py.
"""
